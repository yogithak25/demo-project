pipeline {
    agent any
    stages {
        stage('build') {
            options {
                timeout(time: 10, unit: 'MINUTES' )
            }
            steps {
                sh './build.sh'
            }
        }
    }
}
