pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Building PR: ${env.CHNAGE_ID}"
            }
        }
        stage('Test') {
            steps {
                sh 'echo Running tests'
            }
        }
    }
    post {
        success {
            echo "PR validation passed"
        }
        failure {
            echo "PR Validation Failed"
        }
    }
}
